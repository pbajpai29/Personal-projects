# columbia-climate-risk — dependencies
# Install: pip install -r requirements.txt

# Data I/O
pandas>=2.0
numpy>=1.24
requests>=2.28

# ML
scikit-learn>=1.3
# shap>=0.43  — optional; install separately if cmake is available (pip install shap)
scipy>=1.11

# Dashboard
streamlit>=1.35
plotly>=5.18

# Utilities
python-dotenv>=1.0
sodapy>=2.2          # Socrata client for data.ny.gov MTA datasets
